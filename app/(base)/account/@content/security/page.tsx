import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Security | Settings',
  description: 'Manage your security settings',
};

export default function AccountSecurityPage() {
  return <section>Security</section>;
}
